# Copy this file to secrets.yaml (or merge these keys into your existing one) and fill in real
# values. secrets.yaml is gitignored - never commit your real credentials.

wifi_ssid: "MyHomeWiFi"
wifi_password: "correct-horse-battery-staple"

# password for the fallback hotspot the device broadcasts if it can't reach your WiFi - min. 8 characters
ap_fallback_password: "changeme123"

# generate with: python3 -c "import base64,os; print(base64.b64encode(os.urandom(32)).decode())"
api_encryption_key: "REPLACE_WITH_GENERATED_32_BYTE_BASE64_KEY"

ota_password: "changeme_ota_password"
